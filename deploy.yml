---
- name: Deploy a pyc file
  hosts: all
  remote_user: vikrant
  tasks:
    - name: Installing python package
      apt:
        name: python3
        state: present
    - name: Copy .pyc file to home directory
      copy:
        src: ./calc.pyc
        dest: ~/
        mode: '0755'
